// Site-wide configuration values that are safe to keep in source.

// Cloudflare Web Analytics site token. Empty string = no beacon rendered.
// To enable: Cloudflare dashboard → Analytics & Logs → Web Analytics →
// Add site (lithium-dreams.com), copy the token from the snippet, paste it
// here, and push. The token is a public value (it ships in the page HTML).
export const CF_ANALYTICS_TOKEN = '';
